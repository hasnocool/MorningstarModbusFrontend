# v0.3.1 release preparation

This release prepares the frontend package metadata for v0.3.1 after the controller-native telemetry history and wall display restoration landed on `main`.

The user-facing release documentation is updated after the GitHub release is published so the README can point to the final published version rather than a planned release.
